# Log report: 14 slots, PARTL, ActionScript fingerprint 17272fa697373f11, degraded ban on Remituli

**Date / window:** 2026-04-28, ~18:38:22 – 19:44:31  
**Source log:** `C:\Users\carls\Pictures\farm\Automated-Transformice-user-ban\log.txt`  
**Profile:** 14 slots, `TFMProxyLoader.swf`, `game_version='920'`, `PACKET_AUTO_LOGIN=True`, `BOT_AUTO_PONG=both`, effective Flash stagger **5.0s** (env `BOT_UI_FLASH_LAUNCH_STAGGER_SEC=2.5s` + auto minimum for 14 slots).

## Executive summary

Two consecutive runs show the same failure mode: **most MAIN upstream sockets close with `clean-eof`** during `startup`, `partl_retry`, `room_list`, `player_list`, and `idle`. Startup warns that **`TFM_SECRETS_GAME_VERSION=920` cannot be verified** in the loader SWF (`literal_version_in_swf=False`). **ActionScript** dialogs (**`Error de ActionScript:`**, fingerprint **`17272fa697373f11`**) repeat across slots; the bot auto-dismisses **Continuar** but the underlying issue persists.

After retries, a minority of slots stay **OK** long enough to fetch the room list and enter `*racing1`, but **JoinRoom / player list** drops more MAIN sessions. The **ban round** completes with only **2 live senders** (slots **10** and **11**) vs **12 skipped (PARTL)** — far below typical quorum (**~11** reports, `BOT_BAN_QUORUM_REPORTS`; see `docs/BAN_QUORUM_TRANSFORMICE.md`). Sessions end with **Ctrl+C** and Flash cleanup (including **taskkill** after second Ctrl+C).

## Session A (~18:38 – 19:36)

| Checkpoint | Result |
|------------|--------|
| Network preflight | OK — TCP `51.38.60.113:12801` |
| After first login phase | **OK=4, PARTL=10** |
| Login diagnostics | `AS_dialogs_closed=29`, `AS_unique_fp=1`, `literal_version_in_swf=False`, `loader_embed_verifiable=False` |
| After 3 PARTL retry rounds | **OK=6, PARTL=8** |
| Post-login AS sweep | 12 dialogs closed; health **OK 6→6**, **PARTL 8→8** |
| Room list | OK — **32 rooms**, source slot **4**, **6/14** live |
| Later | Cascading **clean-eof** during `room_list`; slot 6 also **`ConnectionResetError`** |
| Exit | **Ctrl+C** ~19:36; session report path logged under farm `logs\session_report_*.md` |

## Session B (~19:37 – 19:44)

| Checkpoint | Result |
|------------|--------|
| After first login phase | **OK=3, PARTL=11** (slots **12–14** OK initially) |
| After 3 PARTL retry rounds | **OK=5, PARTL=9** |
| Post-login AS sweep | **11** dialogs closed |
| Room list | OK — **37 rooms**, source slot **3**, **5/14** live |
| Room / target | Entered **`*racing1`**; player **15** → **`Remituli#0000`** |
| Pre-ban join | Leader slot **3** + stagger; **slots 3, 7, 9** lose MAIN in **`player_list`** (~10s after joins) |
| Ban round | **2** sends OK (slots **10**, **11**); **12** skipped — **`no upstream`**; quorum warning |
| Exit | **Ctrl+C** ~19:44; cleanup + **taskkill**; late **clean-eof** / **ConnectionResetError** on slots **10–11** |

## Technical notes from the log

- **PARTL driver text (repeated):** `clean_eof:login_often=FLASH_STAGGER+AS;room_often=PRE_BAN+leader_not_for_login_batch` — bot attributes early mass PARTL to **Flash stagger + ActionScript**, not pre-ban room settings.
- **Satellite:** Slot 14 preferred SAT **50250** busy → **50312**.
- **Retry churn:** Many lines **`no fresh bind_ip`** — same `bind_ip` reused per row.
- **Ban quorum:** Log states typical **11** distinct in-room **`/ban`** contributions needed; **2** successful sends will not meet that bar under normal rules.

## Recommended mitigations (aligned with log warnings)

1. **Re-dump / align** `TFM_PROXY_SWF` and **`TFM_SECRETS_*` / `TFM_SECRETS_GAME_VERSION`** with the current live Transformice web client until loader verification is no longer ambiguous.
2. **Raise `BOT_UI_FLASH_LAUNCH_STAGGER_SEC`** above the automatic floor for 14 slots if PARTL at login remains high after (1).
3. Keep **leader-only room join** + **`BOT_PRE_BAN_ROOM_JOIN_STAGGER_SEC`** for **room** phase; they do not fix early login PARTL.
4. Expect **ban effectiveness** only when enough slots maintain **live MAIN** through room sync; see **`docs/BAN_QUORUM_TRANSFORMICE.md`**.

## Related artifacts in this repo

- `logs/BUG_REPORT_2026-04-28_PARTL-actionscript-ban-quorum.md` — same theme, condensed bug framing  
- `logs/SESSION_REPORT_2026-04-28_16-19_16-29.md` — earlier session, as_sweep / full upstream loss variant  
- `docs/BAN_QUORUM_TRANSFORMICE.md` — quorum expectations  
- `docs/LOG_WIFI_VS_MOBILE_ANALYSIS.md` — optional environmental context  

---

*Hand-authored analysis from the pasted log excerpt (2026-04-28). Not a server-side root-cause proof.*
