# Session report: 14 slots, PARTL cascade, full upstream loss before ban

**Date / window:** 2026-04-28, ~16:19:58 – 16:29:36  
**Log host path:** `C:\Users\carls\Pictures\farm\Automated-Transformice-user-ban\log.txt`  
**Profile:** 14 slots, `PACKET_AUTO_LOGIN=True`, `BOT_AUTO_PONG=both`, `game_version='920'`, `TFMProxyLoader.swf`, effective Flash stagger **5.0s** (`BOT_UI_FLASH_LAUNCH_STAGGER_SEC` 2.5s + auto floor for 14 slots).

## Executive summary

Startup and per-account **login succeeded** for all 14 clients, but **MAIN upstream** dropped on most slots (`clean-eof`) during and after login. After **3 PARTL retry rounds**, status was **6 OK / 8 PARTL**. A **post-login ActionScript sweep** closed **11** error dialogs and coincided with **MAIN loss** on at least slots **3** and **6** (`operator_phase=as_sweep`). Brief **room list success** (23 rooms via slot 4) did not last: by room entry and ban time **0/14 slots** had a MAIN/sat **write path**. **Ban round skipped all 14**; quorum **0** sends vs typical need **~11** (`BOT_BAN_QUORUM_REPORTS`). Session ended with **Ctrl+C** during Flash cleanup.

## Notable startup lines

| Item | Value |
|------|--------|
| Network preflight | OK — TCP `51.38.60.113:12801` |
| Loader verification | **WARNING:** cannot verify `TFM_SECRETS_GAME_VERSION=920` in SWF (`literal_match=False`, no version hints in payload) |
| Slot 14 SAT | Port **50250** busy → **50312** |
| `PROXY_LISTEN_USE_ACCOUNT_BIND_IP` | False — per-row bind for Proxifier only |

## Login phase outcomes

- **After first login batch:** **OK=3, PARTL=11** (slots **12–14** OK initially).
- **Fingerprint:** first AS error `17272fa697373f11` on slot 1 (~16:20:25).
- **Diagnostics (`after_login_phase`):** `AS_dialogs_closed=31`, `AS_unique_fp=1`, `incorrect_version_dialogs=0`, `literal_version_in_swf=False`, `loader_embed_verifiable=False`, `cfg_game_version='920'`.

## Retry rounds

- **Round 1/3:** relaunched slots 1–11; many `no fresh bind_ip` — same bind reused.
- **Round 2/3:** churn continued; previously OK slots (**12–14**) pulled into PARTL relaunch set.
- **Round 3/3:** subset of still-failing slots.

**After retries (`after_partl_retry`):** **OK=6, PARTL=8.**

| OK | PARTL (examples) |
|----|------------------|
| 3, 4, 6, 7, 10, 11 | 1, 2, 5, 8, 9, 12, 13, 14 |

## Post-login AS sweep (critical)

- Sweep: **3 passes**, closed **11** ActionScript dialogs (`&Continuar`).
- Immediately after, **MAIN session ended** with `phase=**as_sweep**` at least for **slot 3** and **slot 6** — consistent with dismiss actions correlating with upstream teardown, not only “unblocking” the UI.

## Room list and later

- **16:27:00** — `Room list fetch OK: rooms=23`, `source_slot=4`, `live_slots_after=4/14`.
- Further **clean-eof** during `room_list` on slots 4, 7, 10, 11, etc.
- **16:28:07** — `Room entered directly: '*racing1'`.
- **16:28:09 – 16:28:27** — `Upstream wait: 0/14 slots with write path` (20s) — **no** live MAIN/sat for player list / JoinRoom.
- **16:28:27** — upstream dead snapshot: **all 14** slots last-known **clean-eof** with mixed phases (`partl_retry`, `room_list`, `as_sweep`).
- **16:29:05** — ban round: **skipped 14/14** (`no upstream`), **OK sends = 0**.

## Ban / quorum

- Target user in log: **`zizao#0000`** (manual entry after no player list).
- **`Quorum: typical in-room reports needed = 11`** — this round **OK sends = 0** — below threshold (`docs/BAN_QUORUM_TRANSFORMICE.md`).

## Recommended follow-ups (aligned with log text)

1. **Re-dump / align** `TFM_PROXY_SWF` and **`TFM_SECRETS_*` / `TFM_SECRETS_GAME_VERSION`** with the live web client until verification is no longer ambiguous (`literal_version_in_swf` / loader checks).
2. **Reduce parallel Flash load** — fewer slots or **raise** `BOT_UI_FLASH_LAUNCH_STAGGER_SEC` **above** the auto floor (log suggests this for mass PARTL at login).
3. **Revisit post-login AS sweep** if `as_sweep` continues to align with MAIN drops; root fix is fewer AS errors, not only faster dismiss.
4. **`BOT_PRE_BAN_ROOM_JOIN_STAGGER_SEC` / leader-only** help **room** concurrency; they do **not** fix early PARTL from stagger/AS/loader issues.

## Related docs

- `docs/BAN_QUORUM_TRANSFORMICE.md` — quorum expectations  
- `logs/BUG_REPORT_2026-04-28_PARTL-actionscript-ban-quorum.md` — related PARTL/AS/quorum analysis (may reference a different slice of the same day)  
- `docs/LOG_WIFI_VS_MOBILE_ANALYSIS.md` — environmental notes if relevant  

---

*Parsed from user-provided console log export; session interrupted by user (Ctrl+C) during Flash shutdown.*
