# Log analysis report

**Captured window (local time):** **2026-04-28 11:08** → **2026-04-28 11:25** — matches filename **`2026-04-28-11-08_11-25.md`** (`yyyy-mm-dd-HH-mm_HH-mm` start, then end `HH-mm` same day).

| Field | Value |
|-------|--------|
| Start | First timestamp in the Wi‑Fi capture (`11:08:18`) |
| End | Last meaningful line in the mobile-hotspot capture (~`11:25:31` cleanup) |

Operator-provided captures (same farm, **14 slots**, upstream preflight **`51.38.60.113:12801`** OK both times). Session output lives in repo-root **`log.txt`**; **`logs/`** holds these reports only.

Original machine path: `...\Automated-Transformice-user-ban\log.txt`.

---

## Run A — normal Wi‑Fi

| Item | Detail |
|------|--------|
| When (log) | Starts **11:08:18** |
| Network | Stable Wi‑Fi (`Network preflight: OK`) |
| Flash stagger | Effective **~3.9s** between clients (`BOT_UI_FLASH_LAUNCH_STAGGER_SEC` floor for 8+ slots) |
| End of login phase (**11:10:43**) | **OK = 6** \| **PARTL = 8** |
| Retry outcome (**11:15:21**, after rounds) | **OK = 4** \| **PARTL = 10** (slots still dropping MAIN after relaunch) |
| Notable | Mass `clean-eof`, recurring **Error de ActionScript** (`Continuar`), post-login sweep closed many dialogs |

---

## Run B — mobile data (phone hotspot)

| Item | Detail |
|------|--------|
| When (log) | Starts **11:16:33** |
| Network | Phone hotspot (`Network preflight: OK`) |
| Sat port remap | **`Slot 13: satellite port 49900 was busy or reserved; using 49976`** |
| Slot 1 | **Early loader retries** (`no MAIN TCP` → re-click) before MAIN accept (~26s) |
| Slot 2 | **`ConnectionResetError('Connection lost')`** on MAIN (mobile-style transport reset) |
| End of login phase (**11:19:09**) | **OK = 4** \| **PARTL = 10** |
| Retry failure mode | Slot **10**: MAIN accepted, **`LoginPacket` sent**, MAIN ended **`login+n/a`** before **`LoginSuccess`** → wait spun until timeout (**`Retry: slot 10 still waiting for login … MAIN_TCP=yes`**); **fixed in repo** via **`login_aborted_event`** |

---

## Comparison (same root cause family)

Both runs:

- Failures cluster as **PARTL** (`login ok but upstream closed` / `clean-eof`), diagnostics cite **`FLASH_STAGGER+AS`** and tooling notes for room phase — **sequential Flash + overlapping MAIN** is the main lever, not upstream DNS.
- **ActionScript** dialogs repeat until loader/SWF/version issues are addressed; auto-dismiss is mitigation only.

**Wi‑Fi vs hotspot:** Wi‑Fi had slightly **more** first-pass OK slots (6 vs 4); hotspot shows **port busy**, **slower first MAIN**, and **connection lost** resets typical of **CGNAT / radio**. Neither log shows preflight failure.

---

## `logs/` naming

- **`yyyy-mm-dd-HH-mm_HH-mm.md`** — start datetime (date + time to the minute), underscore, end time (`HH-mm`, same calendar day).
- Optional: copy excerpts from root **`log.txt`** here as `*.log` (ignored; see **`logs/.gitignore`**).

See also: `docs/LOG_WIFI_VS_MOBILE_ANALYSIS.md`.
